// memfrobber calls memfrob on the given string
// memfrobbers job is to call strlen first on the input string, then memfrob() the whole thing 
#include <sys/types.h>

// struct frobber;

// struct frobber *frobthat(char *s);

void* memfrobber(char s[]) {